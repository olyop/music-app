mutation rmUserArtist(
  $userId: Uuid!
  $artistId: Uuid!
) {
  rmUserArtist(
    userId: $userId
    artistId: $artistId
  ) {
    artistId
    name
    photo(size: FULL)
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
  }
}
