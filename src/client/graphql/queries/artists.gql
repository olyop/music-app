query artists(
  $userId: Uuid!
  $page: PositiveInt!
  $orderBy: ArtistOrderByInput!
) {
  artists(
    page: $page
    orderBy: $orderBy
  ) {
    name
    artistId
    numOfSongs
    numOfAlbums
    photo(size: MINI)
    inLibrary(userId: $userId)
  }
}
