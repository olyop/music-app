query genrePage(
  $userId: Uuid!
  $genreId: Uuid!
) {
  genre(genreId: $genreId) {
    genreId
    name
    inLibrary(userId: $userId)
    songs {
      songId
      title
      mix
      duration
      inLibrary(userId: $userId)
      featuring {
        artistId
        name
      }
      remixers {
        artistId
        name
      }
      artists {
        artistId
        name
      }
      album {
        albumId
        title
        cover(size: HALF)
      }
      genres {
        genreId
        name
      }
    }
  }
}
