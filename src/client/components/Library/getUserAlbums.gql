query getUserAlbums(
	$userId: Uuid!
	$page: NonNegativeInt!
	$orderBy: AlbumOrderByInput!
) {
	user(userId: $userId) {
		userId
		albums(
			page: $page
			orderBy: $orderBy
		) {
			albumId
			title
			released
			cover(size: HALF)
			artists {
				artistId
				name
			}
		}
	}
}