mutation addSong(
  $album: ID!,
  $mix: String!,
  $audio: Upload!,
  $title: String!,
  $genres: [ID!]!,
  $artists: [ID!]!,
  $remixers: [ID!]!,
  $discNumber: Int!,
  $duration: String!,
  $featuring: [ID!]!,
  $trackNumber: Int!,
) {
  addSong(
    mix: $mix,
    audio: $audio,
    title: $title,
    album: $album,
    genres: $genres,
    artists: $artists,
    remixers: $remixers,
    duration: $duration,
    featuring: $featuring,
    discNumber: $discNumber,
    trackNumber: $trackNumber,
  ) {
    id
    title
    mix
    duration
    featuring {
      id
      name
    }
    remixers {
      id
      name
    }
    artists {
      id
      name
    }
    genres {
      id
      name
    }
    album {
      id
      title
    }
  }
}
