mutation addUserGenre(
  $userId: Uuid!
  $genreId: Uuid!
) {
  addUserGenre(
    userId: $userId
    genreId: $genreId
  ) {
    name
    genreId
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
  }
}
