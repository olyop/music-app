mutation addUserArtist(
  $userId: Uuid!
  $artistId: Uuid!
) {
  addUserArtist(
    userId: $userId
    artistId: $artistId
  ) {
    artistId
    name
    photo(size: FULL)
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
  }
}
