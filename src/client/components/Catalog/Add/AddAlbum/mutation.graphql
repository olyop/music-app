mutation addAlbum(
  $title: String!,
  $released: Int!,
  $artists: [ID!]!  
) {
  addAlbum(
    title: $title,
    released: $released,
    artists: $artists
  ) {
    id
    title
    released
    artists {
      id
      name
    }
  }
}
