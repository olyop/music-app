mutation addUserAlbum(
  $userId: Uuid!
  $albumId: Uuid!
) {
  addUserAlbum(
    userId: $userId
    albumId: $albumId
  ) {
    albumId
    title
    cover
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
    artists {
      artistId
      name
    }
  }
}
