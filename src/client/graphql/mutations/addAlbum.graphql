mutation addAlbum(
  $cover: Upload!,
  $title: String!,
  $artists: [ID!]!,
  $released: String!,
) {
  addAlbum(
    cover: $cover,
    title: $title,
    artists: $artists,
    released: $released,
  ) {
    id
    title
    released
    artists {
      id
      name
    }
  }
}
