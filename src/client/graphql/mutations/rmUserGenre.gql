mutation rmUserGenre(
  $userId: Uuid!
  $genreId: Uuid!
) {
  rmUserGenre(
    userId: $userId
    genreId: $genreId
  ) {
    genreId
    name
    numOfSongs
    dateAdded(userId: $userId)
    inLibrary(userId: $userId)
  }
}
