mutation removeUserSong($userId: ID!, $songId: ID!) {
  removeUserSong(userId: $userId, songId: $songId) {
    id
    inLibrary
    dateCreated
    song {
      id
      title
      mix
      duration
      featuring {
        id
        name
      }
      remixers {
        id
        name
      }
      artists {
        id
        name
      }
      genres {
        id
        name
      }
      album {
        id
        title
      }
    }
  }
}
