import React from "react"

import { Loading as bem } from "../../globals/bem"

import "./index.scss"

const Loading = () => (
  <div className={bem("")}>
    Loading....
  </div>
)

export default Loading
