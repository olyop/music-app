mutation removeUserAlbum($userId: ID!, $albumId: ID!) {
  removeUserAlbum(userId: $userId, albumId: $albumId) {
    id
    album {
      id
      title
      artists {
        id
        name
      }
    }
  }
}
