mutation rmUserGenre(
  $userId: Uuid!
  $genreId: Uuid!
) {
  rmUserGenre(
    userId: $userId
    genreId: $genreId
  ) {
    genreId
    name
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
  }
}
