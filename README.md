# boilerplate

boilerplate

```{bash}
git clone git@github.com:olyop/boilerplate.git _ & cd _ && attrib -h .git && rd /s /q .git && npm i && git init && git add . && git commit -m "Initial commit" && git remote add origin git@github.com:olyop/_.git && git push -u origin master && code . && exit
```
