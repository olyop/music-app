mutation addUserGenre(
  $userId: Uuid!
  $genreId: Uuid!
) {
  addUserGenre(
    userId: $userId
    genreId: $genreId
  ) {
    genreId
    dateAdded(userId: $userId)
    inLibrary(userId: $userId)
  }
}
