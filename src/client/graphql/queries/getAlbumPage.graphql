query getAlbumPage($albumId: ID!, $userId: ID!) {
  album(albumId: $albumId) {
    id
    title
    cover
    released
    inLibrary(userId: $userId)
    artists {
      id
      name
    }
    songs {
      id
      mix
      title
      duration
      discNumber
      trackNumber
      inLibrary(userId: $userId)
      featuring {
        id
        name
      }
      remixers {
        id
        name
      }
      artists {
        id
        name
      }
      genres {
        id
        name
      }
    }
  }
}
