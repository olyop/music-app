mutation addAlbum(
  $title: String!,
  $released: Int!,
  $artists: [ID!]!  
) {
  addAlbum(
    title: $title,
    artists: $artists,
    released: $released
  ) {
    id
    title
    released
    artists {
      id
      name
    }
  }
}
