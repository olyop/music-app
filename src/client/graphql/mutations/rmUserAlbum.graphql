mutation rmUserAlbum($userId: ID!, $albumId: ID!) {
  rmUserAlbum(userId: $userId, albumId: $albumId) {
    id
    title
    cover
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
    artists {
      id
      name
    }
  }
}
