query getUserAlbums($userId: Uuid!) {
  user(userId: $userId) {
    userId
    albums {
      albumId
      title
      cover
      inLibrary(userId: $userId)
      dateAdded(userId: $userId)
      artists {
        artistId
        name
      }
    }
  }
}
