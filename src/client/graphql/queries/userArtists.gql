query userArtists($userId: Uuid!) {
  user(userId: $userId) {
    userId
    artists {
      artistId
      name
      photo(size: MINI)
      dateAdded(userId: $userId)
      inLibrary(userId: $userId)
    }
  }
}
