mutation addUserAlbum(
  $userId: Uuid!
  $albumId: Uuid!
) {
  addUserAlbum(
    userId: $userId
    albumId: $albumId
  ) {
    albumId
    title
    cover(size: FULL)
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
    artists {
      artistId
      name
    }
  }
}
