mutation rmUserAlbum(
  $userId: Uuid!
  $albumId: Uuid!
) {
  rmUserAlbum(
    userId: $userId
    albumId: $albumId
  ) {
    albumId
    title
    cover
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
    artists {
      id
      name
    }
  }
}
