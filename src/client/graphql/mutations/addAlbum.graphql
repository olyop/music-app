mutation addAlbum(
  $cover: Upload!,
  $title: String!,
  $artists: [ID!]!,
  $released: String!,
) {
  addAlbum(
    title: $title,
    cover: $cover,
    artists: $artists,
    released: $released,
  ) {
    id
    title
    artists {
      id
      name
    }
  }
}
