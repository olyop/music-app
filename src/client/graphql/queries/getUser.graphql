query getUser($id: ID!) {
  user(id: $id) {
    id
    name
    nowPlaying {
      id
    }
    artists {
      id
    }
    albums {
      id
    }
    genres {
      id
    }
    songs {
      id
    }
  }
}
