query getArtistPage(
	$userId: Uuid!
	$artistId: Uuid!
) {
	artist(artistId: $artistId) {
		name
		artistId
		playsTotal
		songsTotal
		albumsTotal
		photo(size: FULL)
		inLibrary(userId: $userId)
	}
}