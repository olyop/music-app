query getUserAlbums($id: ID!) {
  user(id: $id) {
    id
    inLibrary
    dateCreated
    albums {
      id
      album {
        id
        title
        artists {
          id
          name
        }
      }
    }
  }
}
