type Artist {
  id: ID!
  name: String!
  songs: [Song!]!
  albums: [Album!]!
}

type Album {
  id: ID!
  title: String!
  released: Int!
  songs: [Song!]!
  artists: [Artist!]!
}

type Genre {
  id: ID!
  name: String!
  songs: [Song!]!
}

type Song {
  id: ID!
  mix: String!
  album: Album!
  title: String!
  duration: Int!
  discNumber: Int!
  trackNumber: Int!
  genres: [Genre!]!
  artists: [Artist!]!
  remixers: [Artist!]!
  featuring: [Artist!]!
}

type Playlist {
  id: ID!
  name: String!
  songs: [Song!]!
}

type Play {
  id: ID!
  user: User!
  song: Song!
}

type UserArtist {
  id: ID!
  user: User!
  plays: [Play!]!
  artist: Artist!
  numOfPlays: Int!
  inLibrary: Boolean!
}

type UserAlbum {
  id: ID!
  user: User!
  album: Album!
  plays: [Play!]!
  numOfPlays: Int!
  dateCreated: Int!
  inLibrary: Boolean!
}

type UserSong {
  id: ID!
  user: User!
  song: Song!
  plays: [Play!]!
  numOfPlays: Int!
  dateCreated: Int!
  inLibrary: Boolean!
}

type User {
  id: ID!
  name: String!
  prev: [Song!]!
  current: Song!
  next: [Song!]!
  queue: [Song!]!
  plays: [Play!]!
  songs: [UserSong!]!
  albums: [UserAlbum!]!
  artists: [UserArtist!]!
  playlists: [Playlist!]!
}

type Mutation {
  userPrev(
    userId: ID!
  ): User!
  userPlay(
    userId: ID!
    songId: ID!
  ): User!
  userNext(
    userId: ID!
  ): User!
  userSongNext(
    userId: ID!
    songId: ID!
  ): User!
  userSongLater(
    userId: ID!
    songId: ID!
  ): User!
  userSongQueue(
    userId: ID!
    songId: ID!
  ): User!
  addUserSong(
    userId: ID!
    songId: ID!
  ): UserSong!
  addUserAlbum(
    userId: ID!
    albumId: ID!
  ): UserAlbum!
  addUserArtist(
    userId: ID!
    artistId: ID!
  ): UserArtist!
  removeUserSong(
    userId: ID!
    songId: ID!
  ): UserSong!
  removeUserAlbum(
    userId: ID!
    albumId: ID!
  ): UserAlbum!
  removeUserArtist(
    userId: ID!
    artistId: ID!
  ): UserArtist!
  addArtist(
    name: String!
    photo: Upload!
  ): Artist!
  addGenre(
    name: String!
  ): Genre!
  addAlbum(
    title: String!
    cover: Upload!
    artists: [ID!]!
    released: String!
  ): Album!
  addSong(
    album: ID!
    mix: String!
    genres: [ID!]!
    title: String!
    audio: Upload!
    artists: [ID!]!
    discNumber: Int!
    remixers: [ID!]!
    duration: String!
    trackNumber: Int!
    featuring: [ID!]!
  ): Song!
  addPlaylist(
    user: ID!
    name: String!
    songs: [ID!]!
  ): Playlist!
}

type Query {
  songs: [Song!]!
  albums: [Album!]!
  genres: [Genre!]!
  artists: [Artist!]!
  user(id: ID!): User!
  song(id: ID!): Song!
  album(id: ID!): Album!
  genre(id: ID!): Genre!
  artist(id: ID!): Artist!
}
