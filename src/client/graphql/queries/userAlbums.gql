query userAlbums($userId: Uuid!) {
  user(userId: $userId) {
    userId
    albums {
      albumId
      title
      cover(size: HALF)
      dateAdded(userId: $userId)
      artists {
        artistId
        name
      }
    }
  }
}
