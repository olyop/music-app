mutation userAddSongQueue(
  $userId: Uuid!
  $songId: Uuid!
) {
  userAddSongQueue(
    userId: $userId
    songId: $songId
  ) {
    userId
    queue {
      songId
      mix
      title
      duration
      featuring {
        artistId
        name
      }
      remixers {
        artistId
        name
      }
      artists {
        artistId
        name
      }
      genres {
        genreId
        name
      }
      album {
        albumId
        title
        cover(size: HALF)
      }
    }
  }
}
