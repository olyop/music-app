mutation addUserGenre(
  $userId: Uuid!
  $genreId: Uuid!
) {
  addUserGenre(
    userId: $userId
    genreId: $genreId
  ) {
    genreId
    name
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
  }
}
