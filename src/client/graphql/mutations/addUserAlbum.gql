mutation addUserAlbum(
  $userId: Uuid!
  $albumId: Uuid!
) {
  addUserAlbum(
    userId: $userId
    albumId: $albumId
  ) {
    albumId
    title
    released
    cover(size: HALF)
    dateAdded(userId: $userId)
    inLibrary(userId: $userId)
    artists {
      artistId
      name
    }
  }
}
