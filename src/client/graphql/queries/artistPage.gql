query artistPage(
  $userId: Uuid!
  $artistId: Uuid!
) {
  artist(artistId: $artistId) {
    name
    artistId
    allPlays
    numOfPlays
    numOfSongs
    numOfAlbums
    photo(size: FULL)
    inLibrary(userId: $userId)
  }
}
