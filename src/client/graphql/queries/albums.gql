query albums(
  $userId: Uuid!
  $page: NonNegativeInt!
  $orderBy: AlbumOrderByInput!
) {
  albums(
    page: $page
    orderBy: $orderBy
  ) {
    albumId
    title
    released
    cover(size: HALF)
    inLibrary(userId: $userId)
    artists {
      artistId
      name
    }
  }
}
