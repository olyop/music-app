mutation addUserArtist($userId: ID!, $artistId: ID!) {
  addUserArtist(userId: $userId, artistId: $artistId) {
    id
    inLibrary
    numOfPlays
    dateCreated
    artist {
      id
      name
      photo
    }
  }
}
