query getUserAlbums($id: ID!) {
  user(id: $id) {
    id
    albums {
      id
      dateCreated
      inLibrary
      album {
        id
        title
        artists {
          id
          name
        }
      }
    }
  }
}
