query getUserAlbums($userId: ID!) {
  user(userId: $userId) {
    id
    albums {
      id
      title
      cover
      inLibrary(userId: $userId)
      dateAdded(userId: $userId)
      artists {
        id
        name
      }
    }
  }
}
