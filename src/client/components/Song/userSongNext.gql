mutation userSongNext(
  $userId: Uuid!
  $songId: Uuid!
) {
  userSongNext(
    userId: $userId
    songId: $songId
  ) {
    userId
    next {
      songId
      mix
      title
      duration
      featuring {
        artistId
        name
      }
      remixers {
        artistId
        name
      }
      artists {
        artistId
        name
      }
      genres {
        genreId
        name
      }
      album {
        albumId
        title
        cover(size: HALF)
      }
    }
  }
}
