query getArtistPage($artistId: ID!, $userId: ID!) {
  artist(artistId: $artistId) {
    id
    name
    photo
    inLibrary(userId: $userId)
    songs {
      id
      title
      mix
      duration
      featuring {
        id
        name
      }
      remixers {
        id
        name
      }
      artists {
        id
        name
      }
      genres {
        id
        name
      }
      album {
        id
        title
        cover
        released
      }
    }
    albums {
      id
      title
      cover
      artists {
        id
        name
      }
    }
  }
}
