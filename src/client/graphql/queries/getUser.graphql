query getUser($id: ID!) {
  user(id: $id) {
    id
    nowPlaying {
      id
    }
    songs {
      id
      song {
        id
      }
    }
    albums {
      id
    }
    playlists {
      id
    }
  }
}
