mutation rmUserAlbum(
  $userId: Uuid!
  $albumId: Uuid!
) {
  rmUserAlbum(
    userId: $userId
    albumId: $albumId
  ) {
    albumId
    title
    cover(size: HALF)
    inLibrary(userId: $userId)
    dateAdded(userId: $userId)
    artists {
      artistId
      name
    }
  }
}
