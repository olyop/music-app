import React from "react"

import { Loading as bem } from "../../globals/bem"

import "./Loading.scss"

const Loading = () => (
  <div className={bem("")}>
    Loading....
  </div>
)

export default Loading
